package com.example.utils;

/**
 * hint: you can try to use util file with functions instead of util class
 */
public class StringUtils {
    
    public static String toResult(Number result) {
        return String.valueOf(result);
    }
}
